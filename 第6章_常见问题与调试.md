# 第6章：常见问题与调试

## 一、策略问题

### ❌ 问题1：策略没有产生任何信号

**症状：**
```
Found no entry signals for whitelisted currencies. Trying again...
```

**原因分析：**

1. **数据不足**
2. **条件太严格**
3. **指标计算错误产生NaN**
4. **逻辑错误**

**排查步骤：**

#### 步骤1：检查数据

```python
class DebugStrategy(IStrategy):
    def populate_indicators(self, dataframe, metadata):
        # 打印数据信息
        print(f"\n=== 分析 {metadata['pair']} ===")
        print(f"数据长度: {len(dataframe)}")
        print(f"时间范围: {dataframe['date'].iloc[0]} 到 {dataframe['date'].iloc[-1]}")
        print(f"最新价格: {dataframe['close'].iloc[-1]}")
        
        # 检查是否有足够数据
        if len(dataframe) < self.startup_candle_count:
            print(f"⚠️ 数据不足！需要 {self.startup_candle_count} 根，实际只有 {len(dataframe)} 根")
        
        # 计算指标
        dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)
        dataframe['ema'] = ta.EMA(dataframe, timeperiod=20)
        
        # 检查NaN值
        nan_count = dataframe['rsi'].isna().sum()
        if nan_count > 0:
            print(f"⚠️ RSI有 {nan_count} 个NaN值")
        
        return dataframe
```

#### 步骤2：检查每个条件

```python
def populate_entry_trend(self, dataframe, metadata):
    # 分别检查每个条件
    cond1 = dataframe['rsi'] < 30
    cond2 = dataframe['ema'] > dataframe['close']
    cond3 = dataframe['volume'] > 0
    
    print(f"\n条件检查 - {metadata['pair']}:")
    print(f"  RSI < 30: {cond1.sum()} 行满足")
    print(f"  EMA > Close: {cond2.sum()} 行满足")
    print(f"  Volume > 0: {cond3.sum()} 行满足")
    print(f"  所有条件: {(cond1 & cond2 & cond3).sum()} 行满足")
    
    # 如果最新K线满足条件，特别标注
    last_row = dataframe.iloc[-1]
    if cond1.iloc[-1] and cond2.iloc[-1] and cond3.iloc[-1]:
        print(f"  ✓ 最新K线满足所有条件！")
    
    dataframe.loc[
        cond1 & cond2 & cond3,
        'enter_long'
    ] = 1
    
    return dataframe
```

#### 步骤3：导出数据分析

```python
def populate_indicators(self, dataframe, metadata):
    dataframe['rsi'] = ta.RSI(dataframe)
    dataframe['ema'] = ta.EMA(dataframe, timeperiod=20)
    
    # 导出最近100根K线
    recent_data = dataframe.tail(100)
    recent_data.to_csv(f'/tmp/debug_{metadata["pair"].replace("/", "_")}.csv')
    
    # 在Excel/Python中分析为什么没有信号
    
    return dataframe
```

**常见解决方案：**

```python
# 1. 处理NaN值
dataframe = dataframe.ffill()  # 前向填充

# 2. 放宽条件
# ❌ 太严格
(dataframe['rsi'] == 30)  # 几乎不可能

# ✅ 合理
(dataframe['rsi'] < 30)

# 3. 确保有足够的启动K线
startup_candle_count = 200  # 确保足够计算指标

# 4. 检查volume条件
# 某些交易所的volume可能为0或很小
(dataframe['volume'] > 0)  # 可能太严格
(dataframe['volume'] > dataframe['volume'].quantile(0.1))  # 更合理
```

---

### ❌ 问题2：回测和实盘结果差异巨大

**症状：**
回测盈利50%，实盘亏损10%

**原因分析：**

#### 1. 未来数据泄露（Look-ahead bias）

```python
# ❌ 错误：使用了未来数据
dataframe['signal'] = (dataframe['close'] > dataframe['close'].shift(-1))
# shift(-1) 会看到"未来"的价格

# ✅ 正确：只使用历史数据
dataframe['signal'] = (dataframe['close'] > dataframe['close'].shift(1))
# shift(1) 只看"过去"的价格
```

#### 2. 订单簿深度和滑点

**问题：**回测用的是K线收盘价，实盘有滑点

**解决：**配置更真实的价格模式

```json
{
    "entry_pricing": {
        "price_side": "other",  // 买入时用卖一价
        "use_order_book": true,
        "order_book_top": 1,
        "check_depth_of_market": {
            "enabled": true,
            "bids_to_ask_delta": 1.2  // 买盘要大于卖盘
        }
    },
    "exit_pricing": {
        "price_side": "other",  // 卖出时用买一价
        "use_order_book": true,
        "order_book_top": 1
    }
}
```

#### 3. 手续费设置不准确

```python
# 在策略中明确设置手续费
class RealFeeStrategy(IStrategy):
    # 不依赖配置文件
    # Binance 现货：0.1%（有BNB抵扣为0.075%）
    # Binance 合约：Maker 0.02%, Taker 0.04%
    
    def bot_start(self, **kwargs):
        # 在回测中覆盖手续费
        if self.config['runmode'].value == 'backtest':
            self.config['exchange']['pair_fee_maker'] = 0.001  # 0.1%
            self.config['exchange']['pair_fee_taker'] = 0.001  # 0.1%
```

#### 4. 市场环境变化

```bash
# 在不同时间段测试
# 牛市策略可能在熊市失效

# 测试多个时间段
freqtrade backtesting --timerange 20230101-20230630  # 上半年
freqtrade backtesting --timerange 20230701-20231231  # 下半年
freqtrade backtesting --timerange 20240101-20240630  # 今年

# 如果只有某个时间段盈利，说明策略不够稳健
```

---

### ❌ 问题3：策略运行缓慢

**症状：**
每个循环耗时10秒以上

**原因与优化：**

#### 1. 计算了不必要的指标

```python
# ❌ 低效：计算了50个指标但只用3个
def populate_indicators(self, dataframe, metadata):
    # 计算一堆指标
    for period in range(5, 200, 5):  # 40个不同周期的EMA！
        dataframe[f'ema_{period}'] = ta.EMA(dataframe, timeperiod=period)
    # ... 更多指标
    return dataframe

# ✅ 高效：只计算需要的
def populate_indicators(self, dataframe, metadata):
    dataframe['ema_12'] = ta.EMA(dataframe, timeperiod=12)
    dataframe['ema_26'] = ta.EMA(dataframe, timeperiod=26)
    dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)
    return dataframe
```

#### 2. 使用循环而不是向量化

```python
# ❌ 低效：Python循环
def custom_indicator(self, dataframe):
    result = []
    for i in range(len(dataframe)):
        if i < 20:
            result.append(0)
        else:
            result.append(dataframe['close'].iloc[i-20:i].mean())
    dataframe['my_ma'] = result
    return dataframe

# ✅ 高效：向量化操作
def custom_indicator(self, dataframe):
    dataframe['my_ma'] = dataframe['close'].rolling(20).mean()
    return dataframe
```

#### 3. 重复计算

```python
# ❌ 低效：每次都重新计算
def populate_entry_trend(self, dataframe, metadata):
    for i in range(10):  # 循环中重复计算
        value = ta.RSI(dataframe, timeperiod=14)
    return dataframe

# ✅ 高效：只计算一次，在 populate_indicators 中
def populate_indicators(self, dataframe, metadata):
    dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)  # 只算一次
    return dataframe
```

#### 4. 过多的交易对

```python
# 如果监控100个交易对，每个循环都会很慢
# 使用 PairList 过滤

"pairlists": [
    {
        "method": "VolumePairList",
        "number_assets": 20,  # 只保留成交量前20的
        "refresh_period": 1800
    }
]
```

---

## 二、数据问题

### ❌ 问题4：数据下载失败

**症状：**
```
Error downloading data for BTC/USDT
```

**解决方案：**

#### 1. 检查网络连接

```bash
# 测试能否访问交易所API
curl https://api.binance.com/api/v3/ping

# 如果在国内，可能需要代理
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
```

#### 2. 检查交易对是否存在

```bash
# 列出交易所支持的交易对
freqtrade list-pairs --exchange binance --quote USDT

# 检查特定交易对
freqtrade list-pairs --exchange binance --pairs BTC/USDT
```

#### 3. API频率限制

```json
{
    "exchange": {
        "name": "binance",
        "ccxt_config": {
            "enableRateLimit": true,  // 启用频率限制
            "rateLimit": 50  // 每个请求间隔50ms
        }
    }
}
```

#### 4. 分批下载

```bash
# 不要一次下载太多
# ❌ 可能失败
freqtrade download-data --pairs $(freqtrade list-pairs --quote USDT | tr '\n' ' ')

# ✅ 分批下载
freqtrade download-data --pairs BTC/USDT ETH/USDT
freqtrade download-data --pairs BNB/USDT ADA/USDT
```

---

### ❌ 问题5：数据缺失或不连续

**症状：**
回测报告显示数据缺失警告

**检查数据完整性：**

```python
import pandas as pd

# 读取数据
df = pd.read_json('user_data/data/binance/BTC_USDT-5m.json')
df['date'] = pd.to_datetime(df['date'], unit='ms')
df = df.sort_values('date')

# 检查时间间隔
df['time_diff'] = df['date'].diff()
expected_diff = pd.Timedelta(minutes=5)

# 找出缺失的时间段
gaps = df[df['time_diff'] > expected_diff]
if len(gaps) > 0:
    print("发现数据缺失:")
    print(gaps[['date', 'time_diff']])
else:
    print("数据完整")

# 检查缺失值
print(f"\n缺失值统计:")
print(df.isnull().sum())
```

**修复方案：**

```bash
# 重新下载数据
freqtrade download-data \
    --exchange binance \
    --pairs BTC/USDT \
    --timeframe 5m \
    --days 90 \
    --erase  # 删除旧数据重新下载
```

---

## 三、运行时问题

### ❌ 问题6：数据库锁定

**症状：**
```
sqlite3.OperationalError: database is locked
```

**原因：**
SQLite 不支持高并发

**解决方案：**

#### 方案1：增加超时时间

```json
{
    "db_url": "sqlite:////path/to/tradesv3.sqlite?timeout=30"
}
```

#### 方案2：升级到 PostgreSQL

```bash
# 1. 安装 PostgreSQL
sudo apt-get install postgresql postgresql-contrib

# 2. 创建数据库
sudo -u postgres createdb freqtrade
sudo -u postgres createuser freqtrader
sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE freqtrade TO freqtrader;"

# 3. 修改配置
```

```json
{
    "db_url": "postgresql://freqtrader:password@localhost:5432/freqtrade"
}
```

#### 方案3：迁移现有数据

```bash
# 导出SQLite数据
freqtrade db-export --db-url sqlite:///tradesv3.sqlite --export-filename trades_backup.json

# 导入到PostgreSQL
freqtrade db-import \
    --db-url postgresql://freqtrader:password@localhost:5432/freqtrade \
    --import-filename trades_backup.json
```

---

### ❌ 问题7：内存占用过高

**症状：**
进程占用几GB内存

**排查和优化：**

#### 1. 减少 startup_candle_count

```python
# ❌ 占用内存多
startup_candle_count = 5000

# ✅ 合理
startup_candle_count = 200  # 通常200-500足够
```

#### 2. 减少缓存数据

```python
# 定期清理缓存
def bot_loop_start(self, current_time, **kwargs):
    # 每小时清理一次缓存
    if current_time.minute == 0:
        if hasattr(self, 'custom_cache'):
            self.custom_cache.clear()
```

#### 3. 减少交易对数量

```json
{
    "max_open_trades": 3,  // 减少同时持仓数
    "pairlists": [
        {
            "method": "VolumePairList",
            "number_assets": 20  // 减少监控的交易对
        }
    ]
}
```

#### 4. 使用更高的时间周期

```python
# 1分钟K线占用内存多
timeframe = '1m'  # 60个数据点/小时

# 5分钟K线占用内存少
timeframe = '5m'  # 12个数据点/小时
```

---

## 四、调试技巧

### 技巧1：启用详细日志

```json
{
    "verbosity": 3,  // 0=ERROR, 1=WARNING, 2=INFO, 3=DEBUG
    "logfile": "logs/freqtrade.log"
}
```

或命令行：

```bash
freqtrade trade --config config.json --verbose
# 或
freqtrade trade --config config.json -vvv  # 最详细
```

### 技巧2：使用 Python 调试器

```python
class DebugStrategy(IStrategy):
    def populate_entry_trend(self, dataframe, metadata):
        # 在特定条件下触发断点
        if metadata['pair'] == 'BTC/USDT':
            import pdb; pdb.set_trace()  # 断点
            
        # 或使用 IPython（更友好）
        # from IPython import embed; embed()
        
        return dataframe
```

### 技巧3：Jupyter Notebook 交互式调试

```python
# notebook.ipynb
from freqtrade.configuration import Configuration
from freqtrade.resolvers import StrategyResolver
from freqtrade.data.history import load_pair_history

# 加载配置和策略
config = Configuration.from_files(['config.json'])
strategy = StrategyResolver.load_strategy(config)

# 加载数据
dataframe = load_pair_history(
    datadir=config['datadir'],
    timeframe='5m',
    pair='BTC/USDT',
    data_format='json'
)

# 逐步执行策略方法
print("原始数据:", dataframe.shape)
dataframe = strategy.populate_indicators(dataframe, {'pair': 'BTC/USDT'})
print("添加指标后:", dataframe.shape)
dataframe = strategy.populate_entry_trend(dataframe, {'pair': 'BTC/USDT'})
print("生成信号:", dataframe['enter_long'].sum())

# 可视化
import matplotlib.pyplot as plt
plt.figure(figsize=(15, 7))
plt.plot(dataframe['close'])
buy_signals = dataframe[dataframe['enter_long'] == 1]
plt.scatter(buy_signals.index, buy_signals['close'], color='green', marker='^', s=100)
plt.show()

# 检查特定行
print(dataframe[dataframe['enter_long'] == 1][['date', 'close', 'rsi', 'ema']].head())
```

### 技巧4：性能分析

```bash
# 使用 Python profiler
python -m cProfile -o profile.stats -m freqtrade backtesting --config config.json --strategy MyStrategy

# 分析结果
python -c "import pstats; p = pstats.Stats('profile.stats'); p.sort_stats('cumulative'); p.print_stats(20)"
```

### 技巧5：模拟特定场景

```python
# 在策略中模拟异常情况
class TestStrategy(IStrategy):
    def populate_indicators(self, dataframe, metadata):
        # 模拟数据缺失
        if metadata['pair'] == 'BTC/USDT':
            dataframe.loc[100:110, 'close'] = None
        
        # 看策略如何处理
        dataframe['rsi'] = ta.RSI(dataframe)
        
        return dataframe
```

---

## 五、常见错误消息

### 错误1：`Unable to create trade for BTC/USDT: Not enough balance`

**原因：**余额不足

**检查：**
```bash
freqtrade balance --config config.json
```

**解决：**
- 降低 `stake_amount`
- 降低 `max_open_trades`
- 充值

### 错误2：`Pair BTC/USDT is not in the pair whitelist`

**原因：**交易对不在白名单中

**解决：**
```json
{
    "exchange": {
        "pair_whitelist": [
            "BTC/USDT"  // 添加到白名单
        ]
    }
}
```

### 错误3：`Strategy MyStrategy not found`

**原因：**策略文件路径不对

**检查：**
```bash
# 确认策略文件存在
ls -l user_data/strategies/MyStrategy.py

# 确认文件名和类名匹配
# 文件名: MyStrategy.py
# 类名: class MyStrategy(IStrategy):
```

### 错误4：`Unexpected error: 'NoneType' object has no attribute 'close'`

**原因：**数据为空或未正确加载

**排查：**
```python
def populate_indicators(self, dataframe, metadata):
    # 添加检查
    if dataframe is None or len(dataframe) == 0:
        print(f"⚠️ {metadata['pair']} 数据为空")
        return dataframe
    
    # ... 正常逻辑
```

---

## 六、调试清单

遇到问题时，按此清单逐项检查：

### 数据相关
- [ ] 数据是否已下载？
- [ ] 数据是否完整？
- [ ] 数据格式是否正确？
- [ ] 是否有足够的历史数据？

### 配置相关
- [ ] 配置文件语法是否正确（JSON格式）？
- [ ] 交易对是否在白名单中？
- [ ] API密钥是否正确？
- [ ] 时间周期是否匹配？

### 策略相关
- [ ] 策略文件名和类名是否匹配？
- [ ] 是否实现了必需的方法？
- [ ] 指标计算是否有NaN值？
- [ ] 买卖条件是否太严格？
- [ ] 是否有语法错误？

### 运行环境
- [ ] Python版本是否 >= 3.11？
- [ ] 依赖包是否都已安装？
- [ ] 是否有足够的磁盘空间？
- [ ] 是否有足够的内存？
- [ ] 数据库是否可写？

---

## 本章小结

本章介绍了常见问题的排查和解决方法：

1. **策略问题**：无信号、回测与实盘差异、运行缓慢
2. **数据问题**：下载失败、数据缺失
3. **运行时问题**：数据库锁定、内存占用
4. **调试技巧**：日志、断点、Jupyter、性能分析
5. **常见错误**：快速定位和解决

掌握这些调试技巧，可以大大提高开发效率。

下一章我们将学习性能优化和最佳实践。

